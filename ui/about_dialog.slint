import { MagicWindow } from "@vivi/magic.slint";
import { AboutView } from "@vivi/magic/experimental/about_view.slint";

export component AboutDialog inherits MagicWindow {
    min-width: 400px;
    min-height: 400px;
    AboutView {
    }
}
